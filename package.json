{
  "name": "ai-success-website",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing website for AI Success Pte. Ltd. — Smart Loans. Real Growth.",
  "scripts": {
    "build": "OUT_DIR=dist node build.js",
    "dev": "node build.js && python3 -m http.server 8080",
    "build:root": "node build.js"
  }
}
